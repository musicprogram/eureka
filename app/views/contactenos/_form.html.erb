<div class="">
  <%= form_for(@contacto) do |f| %>
  <% if @contacto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contacto.errors.count, "error") %> prohibited this contacto from being saved:</h2>

      <ul>
      <% @contacto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h5 class="white1">Nombre</h5>
    <%= f.text_field :nombre, class:'form-control' %>
  </div>
  <div class="field">
    <h5 class="white1">Descripción</h5>
    <%= f.text_area :descripcion, class:'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit"Agregar",class:'btn btn-default btn-lg btn-block' %>
  </div>
<% end %>

</div>